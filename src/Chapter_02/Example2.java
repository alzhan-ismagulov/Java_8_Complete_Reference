package Chapter_02;

/**
 * Это ещё один пример короткой программы. Присводить исходному файлу имя "Example2.java".
 */
class Example2 {
    public static void main(String[] args) {
        int num; //в этой строке кода объявляется переменная с именем num
        num = 100; //В этой строке кода переменной num присваивается значение 100
        System.out.println("Это переменная num: " + num);
        num = num * 2;
        System.out.println("Значение переменной num * 2 равно: " + num);
    }
}
